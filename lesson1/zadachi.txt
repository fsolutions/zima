 Задачи:
 $a = 5; число 
    $b = '05'; строка
    var_dump($a == $b);         // Почему true? - равны после приведения к одному типу
 var_dump((int)'012345');     // Почему 12345? - приведение строки к типу инт
 var_dump((float)123.0 === (int)123.0); // Почему false? - не равны из за того что тип переменных разный === сравнение и по типу и по содержанию
  var_dump((int)0 === (int)'hello, world');  - оператор сравнивает по типу данных и по содержанию, после преобразования в тип инт
  строка получает значение 0 так как не содержит числовое значение, поэтому тру
 
 Замена значений двух переменных:
 <?php
 $a=1;
 $b=2;
 $a=$a+$b; 
 $b=$a-$b;   
 $a=$a-$b;      
 echo $a;
 echo $b;
 ?>